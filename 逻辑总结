todolist中JS部分的逻辑总结：
整个项目是建立在浏览器本地存储之上的，就是数据不是存储到服务器而是存储到浏览器的本地存储localStorage，
然后通过对浏览器元素进行检索，找到需要处理的元素，然后分析localStorage中的数据，找到需要的信息，经过转化后
渲染到页面。
实验步骤：
    第一步:编写获取localStorage数据的函数getData()，返回一个数组。通过localStorage.get()函数来获取对应的数据，因为localStorage中数据是
          json格式的，因此在需要使用JSON.parse()将数据转化为数组形式返回。

    第二步:编写保存数据的函数saveData()，通过JSON.stringify()函数将数组数据转化为json格式，然后存储到localStorage对应的模块中。

    第三步:编写加载函数load()，加载函数主要的功能是①统计加载两个模块事务数量②通过存储在localStorage中的数据的done属性判断，是将数据
          写到正在进行模块还是写到已经完成模块。写入数据通过js的prepend方法写入列表中，包含<li><p><a>三个标签，同时将相应数据写入标签中。

    第四步:对ID=title的元素进行事件绑定，在敲下回车键（回车键的ascll码为13）的时候将数据存储到localStorage然后再渲染页面。

    第五步:编写删除操作，动态绑定每一个li标签下<a>标签，是的按下则从本地存储中删除相应的数据，然后重新渲染界面。

    第六步:正在进行以及已经完成数据互相转换，可以通过数据对象中的done属性在load()函数中实现。